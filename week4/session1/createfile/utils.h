//this is the header file
#ifndef UTILS_H 
#define UTILS_H //
#include <stdlib.h> //library for exit
#include <stdio.h>

FILE * open_file(char filename [], char mode[]);
#endif

// #ifndef UTILS_H allows conditional compilation. must always be followed by an #end if or else error
