#include <stdio.h>

int str_len(char string) {
    //calculate length of string
    int length;
    length = ;
    return length;
}

int main () {
    char string;
    printf("Enter a string: \n");
    scanf("%s", &string);
    printf("The length of th")

}